# App Store Connect の申告内容

栄養ラベル（App のプライバシー情報）と年齢制限レーティングは、
**Web ページでは代替できない**。App Store Connect の選択式フォームで
アプリごとに回答する必要がある。

ここはその回答内容の控え。入力するときに見ながら埋める。
**アプリの実装を変えたらここも直す。** 実装と食い違うと虚偽申告になる。

---

## 全アプリ共通

### 年齢制限レーティング

4アプリとも **4+**。以下のいずれにも該当しないため。

- 暴力表現、性的表現、不適切な言葉
- ギャンブル、コンテスト
- 医療・治療情報
- **無制限のWebアクセス** — 外部リンクはSafariで開く固定URLのみで、
  アプリ内で任意のWebを閲覧する機能はない

**キッズカテゴリには登録しない。**
サードパーティ広告があるアプリは要件を満たさない。
広告のないアプリも、キッズカテゴリは審査が厳しくなるため選ばない。

### アプリ内課金の扱い

課金があっても栄養ラベルの申告対象にはならない。
決済は Apple が処理し、開発者は決済情報を受け取らないため。

---

## ポケタイプ（PokeTypeWheel）

収集はすべて AdMob 経由。アプリ独自の収集・送信はない。

| データ型 | 用途 | ユーザーに紐づく | トラッキング |
|---------|------|----------------|------------|
| デバイスID（IDFA） | サードパーティ広告, 分析 | はい | **はい** |
| 広告データ | サードパーティ広告 | はい | **はい** |
| 製品インタラクション | サードパーティ広告, 分析 | はい | **はい** |
| パフォーマンスデータ | 分析 | はい | **はい** |
| クラッシュデータ | アプリの機能 | いいえ | いいえ |
| おおよその位置情報（IP由来） | サードパーティ広告, 分析 | いいえ | いいえ |

---

## イカブキ（SplatoonSubSpecialQuiz）

AdMob に加えて **Game Center** を使う。ポケタイプより項目が多い。

| データ型 | 用途 | ユーザーに紐づく | トラッキング |
|---------|------|----------------|------------|
| デバイスID（IDFA） | サードパーティ広告, 分析 | はい | **はい** |
| 広告データ | サードパーティ広告 | はい | **はい** |
| 製品インタラクション | サードパーティ広告, 分析 | はい | **はい** |
| パフォーマンスデータ | 分析 | はい | **はい** |
| クラッシュデータ | アプリの機能 | いいえ | いいえ |
| おおよその位置情報（IP由来） | サードパーティ広告, 分析 | いいえ | いいえ |
| ユーザーID | アプリの機能 | はい | いいえ |

**ユーザーID は Game Center の分。**
スコアと実績が Apple のサーバーへ送られ、Game Center の表示名と結びつく。
当方はこの情報を受け取らないが、アプリから送信される以上は申告する。

### Firebase Analytics について

`AnalyticsManager.swift` はイベント設計だけで、**SDK は導入されていない**
（`import FirebaseAnalytics` はコメントアウト、`Analytics.logEvent` の有効な呼び出しは0件）。

**現時点では申告不要。** 実際に SDK を入れたら以下を追加する。

- 製品インタラクション / 使用状況データ
- 診断データ
- デバイスID（Firebase のインスタンスID）

---

## サーモンランガイド（SRBTCG）

**広告なし、外部通信なし。** 収集項目は少ない。

| データ型 | 用途 | ユーザーに紐づく | トラッキング |
|---------|------|----------------|------------|
| （なし） | — | — | — |

**「データを収集していません」を選ぶ。**

判断の根拠:

- 広告 SDK は未導入（`AdManager.swift` はスタブで TODO のみ）
- 機能提供のための外部通信がない
- 音声認識は端末が対応していれば**デバイス内処理**
  （`requiresOnDeviceRecognition = true`）。
  非対応時は iOS 標準機能を通じて Apple が処理するが、
  **これは Apple のサービスであり、開発者による収集ではない**ため申告対象外
- 記録した内容は端末内にのみ保存

**広告を実装したら、ポケタイプと同じ6項目を申告する必要がある。**

---

## コードトーン（GuitarChordTone）

**何も収集しない。**

| データ型 | 用途 | ユーザーに紐づく | トラッキング |
|---------|------|----------------|------------|
| （なし） | — | — | — |

**「データを収集していません」を選ぶ。**

判断の根拠:

- 広告なし、課金なし、分析SDKなし
- 外部通信なし（オフラインで全機能が動く）
- マイク・カメラ・位置情報を使わない
- ATT のプロンプトも出さない

**広告か課金を入れたら、この申告を必ず見直す。**

---

## PrivacyInfo.xcprivacy との整合

栄養ラベルの回答は、アプリに同梱する `PrivacyInfo.xcprivacy` と
**同じ内容にすること。** 食い違うとアップロード時や審査で問題になる。

`UserDefaults` を使っているアプリは、収集の有無にかかわらず
Required Reason API の申告（`CA92.1`）が必要。
これは「データ収集」ではなく「APIの使用理由」なので、
「データを収集していません」と両立する。

| アプリ | `PrivacyInfo.xcprivacy` |
|-------|------------------------|
| ポケタイプ | 作成済み |
| イカブキ | **未作成** |
| サーモンランガイド | **未作成** |
| コードトーン | **未作成** |
